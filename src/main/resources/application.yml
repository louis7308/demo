spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: database-2.cmj5m2fzc8ae.ap-northeast-2.rds.amazonaws.com
    username: admin
    password: MSGMSG2022

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: create

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE